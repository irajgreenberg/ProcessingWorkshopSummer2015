void drawSymbol(float x, float y, float angle)
{
    //Use pushMatrix(), translate(), and rotate() to
    //compose the graphics context
 
    
    
    //Use primitive drawing commands, such as line(),
    //rect(), ellipse(), etc. to draw a symbol or
    //personal logo, centered on the origin and no
    //larger than 100px by 100px
    
    
    
    
    //Use popMatrix() to reset the graphics context
    
    
}
